# To execute this experiment run:
# python run.py +experiment=exp_standard

defaults:
  - override /trainer: default_trainer.yaml
  - override /model: road_segmentation_model.yaml 
  - override /datamodule: road_segmentation_datamodule.yaml 
  - override /optimizer: adam.yaml 
  - override /callbacks: default_callbacks.yaml
  - override /logger: comet.yaml 

seed: 12345

trainer: 
  gpus: 1
  max_epochs: 100_000

datamodule:
  batch_size: 8 
  num_workers: 0 