# @package _global_

# To execute this experiment run:
# python run.py +experiment=exp_simple_unet

defaults:
  - override /model: rs_simple_unet_model.yaml 
   # Color logging 
  - override /hydra/hydra_logging: colorlog
  - override /hydra/job_logging: colorlog

seed: 12345

trainer: 
  gpus: 1
  max_epochs: 100_000

datamodule:
  batch_size: 8 
  num_workers: 0 